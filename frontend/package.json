{
  "name": "email-spam-detection-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "React + Vite dashboard for the Email Spam Detection Agent",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "smoke": "esbuild scripts/render-smoke-test.jsx --bundle --format=esm --platform=node --outfile=node_modules/.cache/smoke.mjs --jsx=automatic --define:import.meta.env={} --external:react --external:react-dom && node node_modules/.cache/smoke.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "esbuild": "^0.24.2",
    "vite": "^5.4.11"
  }
}
